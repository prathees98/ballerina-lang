package org.ballerinalang.semver.checker.cmd;

import io.ballerina.projects.util.ProjectUtils;
public class CmdTest {
    public void get() {
        SemverCmd s = new SemverCmd();
        s.execute();
    }
}
